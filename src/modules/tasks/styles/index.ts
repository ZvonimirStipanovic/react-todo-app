import { createStyles, makeStyles } from '@material-ui/core/styles';

export const tasksStyles = makeStyles(() =>
    createStyles({
        root: {
            flexGrow: 1,
        },
        title: {
            flexGrow: 1,
        },
    })
);
